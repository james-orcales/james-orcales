
# Search

Search operations retain stdlib string semantics over text bounded to one lint input.

# Split And Transform

Split and transform operations own bounded output and retain Unicode behavior.

# Builder

Builder implements bounded io.Writer and fmt.Stringer behavior for lint diagnostics and fixtures.
