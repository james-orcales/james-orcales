
# Type Resolution

Every bare type in a package declaration or function signature resolves to a
predeclared type or a type declared by that package. Qualified types and calls
without a package declaration index remain unjudged.
