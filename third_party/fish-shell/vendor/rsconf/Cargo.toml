# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
name = "rsconf"
version = "0.3.0"
authors = ["Mahmoud Al-Qudsi <mqudsi@neosmart.net>"]
build = "build.rs"
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "The missing cargo API. A sane autoconf w/ build.rs helpers for testing for system headers, libraries, and symbols"
readme = "README.md"
keywords = [
    "autoconf",
    "build-rs",
    "library-discovery",
    "build-tools",
]
categories = [
    "development-tools::build-utils",
    "development-tools::ffi",
    "os",
    "rust-patterns",
]
license = "MIT OR Apache-2.0"
repository = "https://github.com/mqudsi/rsconf/"

[features]
default = []
nightly = []

[lib]
name = "rsconf"
path = "src/lib.rs"

[dependencies.cc]
version = "1.0.69"

[dev-dependencies.once_cell]
version = "1.0"
