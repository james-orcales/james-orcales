import { double, PI } from "utils-lib";

export function multiply(a, b) {
  return a * b;
}

export const MAGIC_NUMBER = double(21);

export function getPi() {
  return PI;
}

export default function add(a, b) {
  return a + b;
}
