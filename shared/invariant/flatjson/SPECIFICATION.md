
# Always

`Always` enforces flat JSON encoder assertions without coverage recording. This dependency direction
lets `shared/invariant/default` import the encoder without an import cycle or another default.
